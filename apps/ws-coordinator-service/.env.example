# ws-coordinator-service environment variables
PORT=3000
HOST=localhost

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=ws_coordinator-service_db

# Service specific settings
SERVICE_NAME=ws-coordinator-service
