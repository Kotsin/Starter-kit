# notification-gateway environment variables
PORT=3000
HOST=localhost

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=notification_gateway_db

# Service specific settings
SERVICE_NAME=notification-gateway
